# Prototype TODO

Live working list for the [vanilla React prototype](prototype/). Tick items off and delete them — this file is not history.

## Open

- [ ] Align prototype ratio identifiers with the canonical schema: `"1:1"` / `"9:16"` / `"16:9"` → `"1x1"` / `"9x16"` / `"16x9"` across [prototype/cast-data.jsx](prototype/cast-data.jsx) and [prototype/cast-screens.jsx](prototype/cast-screens.jsx). Keep the `:` form as a display label only. (PR #11 review.)
